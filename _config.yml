# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Mindful Musings
subtitle: Thoughts on Life, Culture, Productivity & Investing
# email: me@domain.com
description: >- # this means to ignore newlines until "baseurl:"
  Mindful Musings is a space where I share my thoughts on life, culture, productivity, investing, and anything that catches my interest.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://mindfulmusings.netlify.app" # the base hostname & protocol for your site
twitter_username: bchitnavis
github_username: bchitnavis

# Build settings
theme: minimal-mistakes-jekyll
# theme: jekyll-theme-minimal
plugins:
  - jekyll-feed
  - jekyll-include-cache
  - jekyll-seo-tag
  - jekyll-paginate
  - jekyll-archives

minimal_mistakes_skin: "dark"

# Search
search: true
search_full_content: true

# Pagination
paginate: 10
paginate_path: "/page:num/"

# Include _pages directory
include:
  - _pages

# Defaults
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: single
      author_profile: true
      read_time: true
      show_date: true
      share: true
      related: true
  - scope:
      path: "_pages"
      type: "pages"
    values:
      layout: single
      author_profile: true

# Archives (for year/month/category/tag pages)
jekyll-archives:
  enabled:
    - year
    - categories
    - tags
  layouts:
    year: archive-taxonomy
    category: archive-taxonomy
    tag: archive-taxonomy
  permalinks:
    year: /year/:year/
    category: /categories/:name/
    tag: /tags/:name/

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
